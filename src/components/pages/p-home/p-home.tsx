import type { Props } from './p-home.types';

import React from 'react';

const PHome: React.FC<Props> = (props) => {
    return (
        <div>
            HOME
        </div>
    );
}

export default PHome;
